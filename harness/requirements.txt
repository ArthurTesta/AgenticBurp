# Core runtime deps, exact-pinned. For a fully reproducible environment
# (all transitive deps pinned) install the lock instead:
#     pip install -r requirements.lock
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
pydantic==2.13.5
pyyaml==6.0.3

# Optional runtime -- browser XSS validator (validators/browser_xss_validator.py).
# Lazy-imported; skips cleanly if absent. After install run:
#     playwright install chromium
playwright>=1.40
